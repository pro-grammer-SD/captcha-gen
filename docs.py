"""
                        Docs: Learn from examples! (Remove the hashes before the codes to get them up and running!) Copyright (C) 2025 Soumalya Das
"""

""" Code 1: Hello Message """

#from captcha import CaptchaEntity

#ce = CaptchaEntity(True) # try this (output: Hello from Captcha library!)

""" Code 2: Print numeric captcha """

#from captcha import CaptchaEntity

#ce = CaptchaEntity(wm=False) # same as `False`
#ce.generate("numeric")

""" Code 3: Print lettered captcha """

#from captcha import CaptchaEntity

#ce = CaptchaEntity(wm=False)
#ce.generate("lettered")

""" Code 4: Print lettered captcha """

#from captcha import CaptchaEntity

#ce = CaptchaEntity(wm=False)
#ce.generate("lettered")

""" Code 4: Print mixed captcha """

#from captcha import CaptchaEntity

#ce = CaptchaEntity(wm=False)
#ce.generate("mixed")